import React from "react";
import { Form } from "react-bootstrap";

export default Search({ allTags, setAllTags, notes, setNotes, ...props}){
    <Form.Group controlId="search">
        <Form.Control type="text" autoFocus placeHolder="Search Tags"
}
